# 11: Коментарі коду — англійською, одним проходом

**What to build:** `CLAUDE.md` велить писати код і коментарі англійською, а
`.md`-файли українською. Правило з'явилося тоді, коли `app/` і `tools/` уже
були написані з українськими коментарями, тож репозиторій зараз змішаний: у
тікеті 09 нові коментарі й назви тестів уже англійські, сусідні рядки в тих
самих файлах — українські. Тікет закриває розходження одним проходом.

Це прибирання, не зміна поведінки: жоден рядок логіки не рухається, тести
лишаються тими самими тестами. Перевірка — зелений `node --test` до і після
та diff, у якому немає нічого, крім тексту коментарів і назв тестів.

**Межа перекладу проходить не по мові, а по адресату.** Англійською йде те,
що читає розробник; українською лишається те, що читає Тренер або автор
контенту:

- **Перекладати:** коментарі в `app/atlas.mjs`, `app/atlas.test.mjs`,
  `tools/atlas-ids.mjs`, `tools/atlas-ids.test.mjs`, `tools/build-atlas.mjs`,
  `tools/build-atlas.test.mjs`, назви тестів у `test('…')`, повідомлення
  `assert` — разом близько 200 рядків у шести файлах.
- **Не чіпати:** рядки `ContentError` в `app/atlas.mjs` і помилки
  `tools/atlas-ids.mjs` (адресат — автор контенту, який править JSON), вивід
  `tools/review-queue.mjs` (адресат — Тренер на вичитці), словник
  `app/i18n.mjs`, `<title>` в `index.html`, усі значення в `content/`,
  усі `.md`-файли.

**Blocked by:** —

**Status:** resolved

- [x] Коментарі шести названих файлів — англійською
- [x] Назви тестів і повідомлення `assert` — англійською
- [x] Повідомлення про помилки контенту, вивід для Тренера, словник інтерфейсу
      і контент лишилися українськими
- [x] `node --test` дає ту саму кількість тестів, усі зелені
- [x] У diff немає змін логіки — лише текст коментарів і назв
- [x] Жоден файл не лишився змішаним за мовою

## Comments

**Зроблено 21.09.2026.** `node --test` до і після: 51 тест, усі зелені;
`node tools/atlas-ids.mjs --check` проходить. Змінено шість названих файлів;
diff містить лише текст коментарів, назв тестів, повідомлень `assert` — і
повідомлення та вивід самого `tools/build-atlas.mjs`.

**Відхилення від тікета, свідомі:**

- Повідомлення про помилки й вивід `tools/build-atlas.mjs` перекладені
  англійською. У жодному з двох списків тікета їх не було, а адресат у них —
  розробник, що збирає атлас з Figma, тож за принципом тікета це англійська.
- Два рядки, які скрипт **записує в згенерований SVG** (`Див. CREDITS.md`,
  `Зібрано tools/build-atlas.mjs…`), лишилися українськими: це вміст
  артефакту, а не коментар скрипта, і справжні експорти Figma в репозиторії
  відсутні, тож SVG не перегенерувати. Наступне збирання варто зробити з
  англійським коментарем.
- «Жоден файл не змішаний за мовою» виконано для коментарів. Файли, що
  містять повідомлення для автора контенту (`app/atlas.mjs`,
  `tools/atlas-ids.mjs`) і вивід для Тренера (`tools/review-queue.mjs`),
  мають український **рантайм-текст** за умовою тікета, а не за недоглядом.
- З заголовка `app/atlas.mjs` прибрано слова про service worker: його не
  буде (тікет 08 закритий).
