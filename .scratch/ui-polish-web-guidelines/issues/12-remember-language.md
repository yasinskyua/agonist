# 12: Мова інтерфейсу запам'ятовується

**What to build:** Обрана мова зберігається між запусками, тож Тренер, який обрав англійську
для іноземного Клієнта, не перемикає її щоразу. За відсутності збереженого
значення (перший запуск, приватне вікно) мова українська. Найменш потрібний
тікет із набору: можна відкласти.

**Blocked by:** None (can start immediately)

**Status:** done

- [x] Після перемикання на англійську й перезавантаження застосунок відкривається англійською
- [x] Без збереженого значення застосунок відкривається українською
- [x] Заборонений чи очищений браузерний сховок не ламає застосунок: мова просто стає українською
- [x] Ніякі інші стани (екран, висота шторки, пошуковий запит) не зберігаються між запусками, як і було
- [x] `node --test` зелений

## Виконано

`loadLang` / `saveLang` в `app/i18n.mjs` (з тестами): мова лежить у `localStorage` під ключем `lang`; будь-яка помилка доступу чи чуже значення дають українську. `ui.mjs` читає її на старті й пише при перемиканні. Більше нічого не зберігається. Перевірено в Chrome-панелі: чистий старт — `uk`, після перемикання й перезавантаження — `en`.
